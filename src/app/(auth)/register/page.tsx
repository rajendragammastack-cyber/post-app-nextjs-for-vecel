import { RegisterForm } from '@/components/auth/RegisterForm';

export const metadata = {
  title: 'Sign up | Post Management',
  description: 'Create an account',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
